<div class="container">
	<div class="row">
		<div class="col-md-4">
			<%= form_for ([current_user,@theatre, @screen]) do |form| %>
				<div class="form-group">
			        <%= form.label :name, "Screen Name:", class: "col-md-4 control-label" %>
			        <div class="col-md-8">
			          <%= form.text_field :name, class: "form-control" %>
			        </div>
			      </div>
			      <%= form.submit "Submit", class: "btn btn-default btn-primary" %>  
			<%end%>
		</div>
	</div>
</div>
		<%= link_to "Back", user_theatre_path(current_user, @screen.theatre.id), class: "btn btn-default" %>












